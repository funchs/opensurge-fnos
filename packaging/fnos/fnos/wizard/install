[
    {
        "stepTitle": "OpenSurge 网关配置",
        "items": [
            {
                "type": "tips",
                "helpText": "OpenSurge 以旁路由方式工作。网卡名 / 局域网 IP 已预填占位值；<b>若你未修改</b>，安装时会按飞牛网络设置同源数据自动探测（优先 NetworkManager / <code>nmcli</code>，其次 <code>ip</code> 路由）并替换。<b>若你改过</b>，则以你填写的为准。<br>可在「系统设置 → 网络设置」或终端 <code>nmcli device status</code> 确认。"
            },
            {
                "type": "text",
                "field": "wizard_port",
                "label": "Web GUI 端口",
                "initValue": "61767",
                "rules": [
                    { "required": true, "message": "请填写端口号" },
                    { "pattern": "^([1-9][0-9]{2,4})$", "message": "端口必须是 100-65535 之间的数字" }
                ]
            },
            {
                "type": "text",
                "field": "wizard_interface",
                "label": "网关网卡名",
                "initValue": "eth0",
                "rules": [
                    { "required": true, "message": "请填写网卡名" },
                    { "pattern": "^[A-Za-z0-9._-]+$", "message": "网卡名只能包含字母、数字、点、下划线和短横线" }
                ]
            },
            {
                "type": "text",
                "field": "wizard_lan_ip",
                "label": "NAS 局域网 IPv4",
                "initValue": "192.168.1.20",
                "rules": [
                    { "required": true, "message": "请填写 NAS 的局域网 IP" },
                    { "pattern": "^([0-9]{1,3}\\.){3}[0-9]{1,3}$", "message": "请填写合法的 IPv4 地址" }
                ]
            }
        ]
    }
]
