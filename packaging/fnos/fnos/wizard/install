[
    {
        "stepTitle": "OpenSurge 网关配置",
        "items": [
            {
                "type": "tips",
                "helpText": "OpenSurge 以旁路由方式工作。<br><b>网关网卡名</b>和 <b>NAS 局域网 IP</b> 留空时，安装过程会自动探测默认路由网卡及其 IPv4（一般就是你在局域网里访问 NAS 用的那张网卡）。预填不准时再手动改即可。<br>也可在终端用 <code>ip -br link</code> / <code>ip -4 route get 1.1.1.1</code> 自行确认。"
            },
            {
                "type": "text",
                "field": "wizard_port",
                "label": "Web GUI 端口",
                "initValue": "61767",
                "rules": [
                    { "required": true, "message": "请填写端口号" },
                    { "pattern": "^([1-9][0-9]{2,4})$", "message": "端口必须是 100-65535 之间的数字" }
                ]
            },
            {
                "type": "text",
                "field": "wizard_interface",
                "label": "网关网卡名",
                "initValue": "",
                "rules": [
                    { "pattern": "^$|^[A-Za-z0-9._-]+$", "message": "网卡名只能包含字母、数字、点、下划线和短横线；留空则自动探测" }
                ]
            },
            {
                "type": "text",
                "field": "wizard_lan_ip",
                "label": "NAS 局域网 IPv4",
                "initValue": "",
                "rules": [
                    { "pattern": "^$|^([0-9]{1,3}\\.){3}[0-9]{1,3}$", "message": "请填写合法的 IPv4 地址；留空则自动探测" }
                ]
            }
        ]
    }
]
